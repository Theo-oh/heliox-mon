# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 变更

- **延迟采集改为逐包解析**：不再只把 ping 摘要均值当 RTT。默认 20 包，间隔走已有的
  `PING_GAP_MS`（此前该配置从未传给 `ping -i`，实际永远是 1 秒）。成功包 RTT 入库，
  并同时写下 min/avg/max/p95/标准差/`sum_rtt`/`sum_sq`，后续窗口统计不再对「均值的均值」
  求 P95。环境错误仍跳过、全丢包 RTT 仍为 NULL。
- **延迟 7 天降采样按包计算桶 P95**：拼齐 10 分钟内的成功样本再写摘要，随后丢掉 `rtts`。
  旧行没有样本时退回加权平均 / MIN / MAX，P95 留空而不是用均值冒充。降采样改走事务，
  避免 INSERT 成功、DELETE 失败时下次再插出重复桶。
- **`GET /api/latency` 按包合并时间桶**：每个点带 `max_rtt` / `p95` / `recv` / `sum_rtt` /
  `sum_sq`，1 分钟粒度还附带 `rtts` 数组供前端缩放时重算真 P95。窗口 stats 改为包加权，
  日报延迟小节走同一套摘要。
- **延迟指标条按真实样本计算**：24h 有 `rtts` 时平均 / P95 / 最大 / 最小都来自窗口内成功包；
  多目标不再把不同链路捏成一个 P95（数字跟焦点目标走，点胶囊切换）。图上可叠加每桶 P95
  虚线，极值点改标包级 min/max。粗粒度没有逐包时 P95 标「约」。横幅抖动改用最近一轮的
  连续差均值。

- **`GET /api/latency` 支持 gzip**：逐包样本让默认 24h 视图的响应涨到近 1MB（两目标），
  而前端每 60 秒全量重拉一次，走 Tunnel 或移动网络时是持续的 MB 级流量。实测
  934KB → 214KB。SSE（`/api/traffic/realtime`）刻意不套，压缩流会被缓冲、
  把实时推送变成一次性吐出。
- **`/api/latency` 的 `stats.count` 语义变了**：旧口径是「有有效 RTT 的桶数」，
  新口径是窗口内所有桶数（整段无数据时才为 0）。前端不消费该字段，
  但拿它算覆盖率的外部消费者会高报。

- **push main 即发版**：`release.yml` 现在同时由 branch push 和 `v*` tag 触发，产物一致、
  都会成为 `releases/latest`。push main 发的 Release tag 名为 `build-<run_number>`，
  版本号取 `git describe`（形如 `v0.26.2-3-g40910b2`）；打 `vX.Y.Z` 仍发干净版本号的正式版。
  理由是发版 ≠ 部署：VPS 上 `monitor update` 是手动执行的，`latest` 变了线上也不会自己动，
  所以自动发版最坏只是多几个没人拉的 Release，而省掉了"每次都要为部署补一个 tag"的仪式。
- **`release.yml` 增加 `go vet` + `go test` 门禁**：`ci.yml` 与发布工作流并行、拦不住发布，
  而自动发版无人值守，没有门禁就等于把红着的构建推成 `latest`。
- `Makefile` 的版本推导加 `--match 'v[0-9]*'`：自动发版会留下 `build-N` tag，
  不过滤的话下一次 `git describe` 会以它为基准，版本号一轮比一轮长。

### 修复

- **客户端脚本在全丢包时把 `lost` 上报成 0**：全丢包分支的 `printf "... %d 0 []"`
  漏传了刚算好的 `lost` 实参。BSD/macOS awk 下这是 fatal error，配合 `set -e`
  让脚本静默退出、整轮不上报；mawk/gawk 下 `%d` 补 0，于是「一个包都没回来」
  被记成 `sent=N, lost=0`，前端丢包率显示 0%——正是上一条要修的场景。
- **客户端上报的 `rtts` 按排序后的数组序列化**：丢掉了到达顺序，而前端抖动用的是
  相邻样本差均值，对已排序数组它恒等于 `(max-min)/(n-1)`，所有客户端目标的抖动
  都是系统性错误值。现另存一份原序数组用于输出，min/max/P95 仍走排序后的数组。
- **降采样前的旧行拿分钟均值冒充实测最大值**：`fillLegacy` 里的 `Max = Avg` 让升级后
  7 天内的历史原始行、90 天内的历史聚合行在「最大」一栏显示桶平均值。现留 `nil`
  让前端显示 `--`——不知道就说不知道。
- **`PING_GAP_MS` 生效后小于 200ms 的存量配置会让采集全线中断**：iputils 对非 root
  强制 `-i >= 0.2`，否则以退出码 2 失败，而该退出码被归为「执行环境错误」只打日志跳过。
  这个配置项在改动前是空转的，一次更新就会把延迟图变空。现非 root 下夹到 200ms，
  并在首次夹住时提示一次。
- **上报的 `recv`/`sum_rtt`/`sum_sq` 未做任何校验**：这三个字段在没有 `rtts` 时被
  原样当聚合权重写库，一条 `sum_rtt=5e11` 就能永久污染该目标的窗口均值，7 天后
  还会被降采样固化进聚合行。现校验取值范围与 `recv ≤ sent-lost` 的自洽性。
- **指标条的告警阈值没随口径重定，且挂错了指标**：`max > median * 2` 比的原本是桶均值的
  最大值，现在 `max` 已是窗口内单个最差包（24h 约 3 万个样本），角标会长期常亮。
  实测健康 / 抖动 / 薄尾 / 跨洋四类正常链路下，`max > median*2` 与 `max > p95*2` 的
  误报率都是 100%——任何「max 对比中心统计量」的判据在这个样本量下都必然触发，
  而后者在真劣化时反而不报。判据改为 `p95 > median * 2`（四类正常链路全不报、
  真劣化必报），角标随之从「最大」挪到「P95」：单个最差包不可行动，
  P95 抬高才说明这条链路真的变坏了。
- **开启「极值」后缩放，极值点不跟随窗口**：改用 ECharts markPoint 后 `packetExtrema`
  仍扫全量点，放大到不含全局极值的区间时标记会消失或被画在网格外，而同一次渲染里
  y 轴量程只按窗口算。极值点与平均线现均按缩放窗口过滤，与 y 轴口径一致。
- **取消选中目标时会把统计焦点带走**：`statsFocusTag` 在取消选中的分支里也会赋值，
  三目标全选、焦点在 C 时点掉 A，焦点会静默变成不在图上的 A 并回落到第一个目标。
  现只在选中分支里赋值。
- **指标条同一行里混用单目标与多目标口径**：平均/P95/最小/最大只取焦点目标，而丢包与
  异常时长仍对所有选中目标求和，读数无法互相解释。现整行都跟焦点走，图上的丢包底色
  仍用全量序列（那是叠加展示，不是要被读出来的数），并在图例行点破当前指标目标。
- **客户端延迟上报把 curl 超时当成 5000ms 成功**：`--max-time 5` 超时仍输出
  `time_total≈5`，旧脚本只判断 `>0` 就当成功 RTT、丢包为 0。现以 `/api/echo` 的 204
  为成功，000/超时/其他状态计 `lost`，并上报 `rtts`/`max_rtt`/`p95_rtt`；服务端有样本
  数组时按包重算摘要。
- **「本月总流量」文案与实际口径不符**：主卡数值和明细表「本月累计」取的是
  `/api/stats` 的 `this_month`，后端按 `RESET_DAY` 起算的**计费周期**聚合
  （`GetBillingCycleDates`），不是自然月。重置日 12 号时，8 月 9 日看到的是
  07-12 起的累计，与下方「历史趋势」按自然月分组的 08 月柱子对不上，容易被当成数据出错。
  文案改为「本周期总流量」/「周期累计」，与已有的「重置日 N 号」「周期末预估」对齐。
  `this_month` 字段名保持不变（改了会破坏 API 兼容），在前后端注释里标注为历史遗留。
- 后端相关注释与日志文案统一改为「计费周期」，避免下次照着注释误判口径。
- **延迟图十字准星在右轴标出无意义的百分比**：丢包轴量程 0–400 而实际值域只有 0–100%，
  上面 3/4 是留白（贴底色块不遮延迟折线）。`axisLabel` 已刻意不标 100 以上，但 axisPointer
  走的是另一套 formatter，绕开了这个过滤，鼠标抬高就会显示「310.11」这种不存在的丢包率。
  且横向准星只表示鼠标高度，与贴底色块的顶端无关，读数本身也没有含义。现关闭右轴的
  准星标签，横线与左轴 ms 读数保留。
- Service Worker `CACHE_NAME` 补到 v15。

## [0.26.2] - 2026-08-08

### 修复

- **页面常驻占用约 25%~30% GPU**（M4 Mac mini 实测）：`.wrapper` 上的
  `backdrop-filter: blur(40px)` 覆盖整个页面容器——桌面视口下是 1100×2500 CSS px，
  DPR=2 即 1100 万物理像素。而 body 背景两套主题都是纯色（无渐变无图片），模糊一块
  纯色输出还是同一块纯色，再被 0.95 不透明的 `--wrapper-bg` 盖住，渲染结果与不加
  完全一致：整份开销换来零像素差异。代价却被两件事放大成常驻负载——页面有三个
  `infinite` 的 trendPulse 圆点、外加每秒刷新一次的实时网速 canvas，容器区域持续变脏，
  浏览器缓存不住 backdrop 快照，只能每帧重算这 1100 万像素的模糊。移除 `.wrapper`、
  `.card`、`.popover` 三处 `backdrop-filter`（后两者背后同样是近乎不透明的纯色底），
  GPU 占用降到个位数。三个脉冲动画保留：没有大面积模糊后它们只影响自身几百像素的
  合成层。登录页的 `backdrop-filter` 不动——那里背景是动画渐变加浮动光斑，模糊有真实效果。
- 弹层（通知 / 延迟「更多」）底色改用新增的不透明变量 `--popover-bg`：它浮在正文之上，
  原先靠 `backdrop-filter` 打底，沿用半透明的 `--card-bg` 会让下面的文字直接透上来。
- `.card` 的 `transition: all` 收窄为 `border-color, transform`，hover 不再牵动无关属性。
- Service Worker `CACHE_NAME` 补到 v11。

## [0.26.1] - 2026-08-03

### 修复

- **图表 Y 轴顶部留出大片空白**：`niceCeil` 的档位只有 1/2/5，一个数量级三档太粗。
  延迟峰值 275ms 时轴上限被一路抬到 500——调用方本意留 15% 余量，实际留了 58%，
  曲线被压在图底三分之一。档位加密到 1/1.2/1.6/2/2.4/3/4/5/6/8/10（每档都能被 4
  整除成整数，四等分刻度才不出小数），同一份数据轴上限变 400、刻度 0/100/200/300/400。
  流量趋势图共用此函数，同样受益（日峰值 2.5GB 时轴上限由 5GB 收到 3GB）。
  返回值加 `toPrecision(12)` 归整：`1.2 * 100` 在 IEEE754 下是 `120.00000000000001`，
  会漏成轴标签上的一串小数，也会让 `trend.js` 靠 `value === axisMax` 认顶格刻度失效。
- Service Worker `CACHE_NAME` 补到 v10：0.26.0 改了 `chart.js` 却漏了 bump。缓存是
  网络优先策略，漏 bump 只影响离线回退时拿到的版本，不影响联网访问。

## [0.26.0] - 2026-08-03

### 变更

- **延迟图左轴上限改为跟随缩放窗口**：此前按整段数据定死，白天一个 400ms 的尖峰会让
  夜里那段 160ms 的曲线永远被压在图底三分之一，放大看细节这件事就白做了。缩放回调里
  只 merge 左轴而不整图 `setOption`——滚轮会连续触发，重建整张图既卡顿，又要和正在
  进行的手势抢 dataZoom 状态。
- 丢包区间色块透明度 0.1 → 0.06。满幅色块在 24h 全景下是「找得到这一分钟」的唯一
  线索所以保留，但同一层淡红 2px 宽时是标记、150px 宽时就是一堵墙，把 0.24.0 刚
  消除的「丢包把图切成两半」原样搬了回来。

## [0.25.0] - 2026-08-03

### 变更

- **服务重启/更新后不再被踢回登录页**：会话此前只存在进程内存的 `sync.Map` 里，
  一次 `deploy.sh monitor update` 或 systemd 重启就全部蒸发——浏览器还揣着有效期内的
  Cookie，却每次都要重登。改为无状态的 HMAC-SHA256 签名 token（`<过期时间戳>.<签名>`），
  签名密钥首次启动时随机生成并存进 `config` 表，重启后原样读回，已登录的浏览器不受影响。
  只有删库或换机才需要重新登录。
  - 密钥刻意**不**从 `HELIOX_MON_PASS` 派生：那样攻击者拿自己的一个合法 token 就能离线
    爆破管理员密码。
  - 随之删掉了内存 session 表与每小时清理它的 goroutine。
- **会话滑动续期**：剩余有效期不足一半（15 天）时顺带重签 Cookie，持续使用的浏览器
  不会在第 30 天被硬性打回登录页。
- **数据库文件权限收紧到 0600**（含 `-wal`/`-shm`）：库里开始存会话签名密钥，
  不该让同机其他用户读到。此前跟随 umask，通常是 0644。

## [0.24.0] - 2026-08-03

### 修复

- **单桶丢包在图上画不出色块**：`buildLossAreas` 的区间右端画到末桶的**桶头**，
  单个粒度桶的丢包宽度为 0，于是「整分钟全丢」这种最严重的情况反而一个标记都没有——
  图上只剩折线断口，和灰色的「无数据」缺口长得一模一样。右端改为推到桶尾，宽度与
  指标条的「异常时长」口径对齐。
- **指标条口径与标签不符**：指标全部按图表的缩放窗口计算，标签却写死查询范围，
  缩放到 15 分钟仍显示「24h 平均」。缩放后标签改为「选区平均」，图例行补注
  「指标取自当前选区」。横幅的丢包取最近 5 个桶、指标条取选区，两个数字并排出现时
  容易被当成自相矛盾，横幅 meta 补上「丢包近 N 分钟」。

### 变更

- **丢包不再切断延迟曲线**：右轴量程放到 400，100% 丢包只占图高的下 1/4。此前
  丢包顶到天花板，一分钟的抖动就把延迟曲线拦腰截断，视觉权重远超其信息量。
- **丢包柱宽度跟随粒度**：固定 2px 的 `bar` 换成 `custom` 序列，色块宽度按时间坐标
  算，一个粒度桶就是一格。此前放大后柱宽不变，一分钟的丢包看起来像一条分隔线而不是
  一段区间。粒度桶窄于 1px 时仍保底 2px 宽。
- 丢包区间色块由 warn 色改为 danger 色，与灰色的「无数据」在语义上分开；标签写明
  丢包率与时长，且只在连续多桶时显示（单桶散落几十处时逐个挂标签会糊成一片）。
- **Tooltip 补上丢包的原始计数**：`丢包: 40.0% · 2/5 包`。1 分钟粒度的 40% 只是
  5 个包丢了 2 个，10 分钟粒度的 40% 背后是几十个包，没有分母无从判断这个百分比
  值不值得当真。同时延迟行在 RTT 为 null 时由「丢包」改称「无回包」——原文案与
  丢包序列同名，全丢的桶上一条 tooltip 里会出现三个「丢包」。

## [0.23.0] - 2026-08-02

### 新增

- **本地开发免登录**：设 `HELIOX_MON_DEV_NO_AUTH=1` 后跳过登录页，专供调试前端。
  开关只存在于 **darwin 构建**（`internal/config/devauth_darwin.go`），生产的 Linux 二进制里
  没有对应分支；即便开着，服务端也只对**回环地址**放行（只认 `RemoteAddr`，不看可伪造的
  `X-Forwarded-For`）。启动时会打印告警。
- README 补「前端调试」小节：本地起服务的命令、`go:embed` 是编译期快照（**改完前端必须重启
  进程**，只刷新浏览器永远看的是上次启动的那份）、模块图失败是静默的、排查顺序。

### 变更

- **登录页按需加载 Turnstile**：Cloudflare 的脚本与验证部件改为由服务端在
  `HELIOX_TURNSTILE_SECRET` 非空时才注入（`buildLoginPage`）。此前无论有没有配置都会去连
  `challenges.cloudflare.com`，本地/内网/离线环境下登录页会挂着一个连不上的验证框。
  站点公钥同时提取为 `HELIOX_TURNSTILE_SITEKEY`，默认值即原先硬编码的那个，已部署实例
  无需新增配置。

- **延迟监控重做为「状态横幅 + 指标条 + 图」单卡**：原来卡片顶部有一条占两行的控制栏
  （日期输入、四个按钮、目标复选框、三个显示开关），把「链路现在是否正常」这个唯一要紧的
  问题埋在一堆统计卡片里。现在：
  - **链路状态横幅**：`正常 / 劣化 / 中断` 三档，各带呼吸圆点与当前延迟、抖动、丢包读数；
    中断态把「抖动」换成「已持续 X 分钟」、当前延迟显示 `--`。判据全部**相对化**——
    丢包 ≥ 2%，或最近 5 个粒度桶的均值超过区间中位数的 1.5 倍（且至少高出 30ms）才算劣化。
    设计稿建议的「均值 < 200ms 才算正常」没有采用：跨洋链路稳定在 165ms 也会被判成劣化。
    最新采样超过一个缺口阈值没更新、或最近窗口内一个包都没回来，判中断。
  - 控制栏收进标题行：目标筛选胶囊 + `24h | 7 天 | 自定义` 分段控件 + 「更多」弹层
    （日期区间、前后一天、查询/重置、丢包/极值/平均线三个开关）。
  - 6 栏指标条（平均 / P95 / 最小 / 最大 / 丢包率 / 异常时长），最大值与丢包率按阈值转橙红。
  - 图表：高 220px，丢包由折线改为挂右轴的竖条（只画丢包 > 0 的桶），无数据/异常区间色块
    带时长标签并错开两行；去掉底部的 dataZoom 滑块，改为图例行（目标 / 丢包 / 无数据 +
    采样点数与粒度），滚轮缩放与「统计跟随缩放窗口」保持不变。
  - 时间轴刻度补 formatter：ECharts 默认在日级刻度上只写一个「2」，看不出是几号还是几点。
  - `latency.js` 拆成 `latency/{index,chart,stats,palette}.js`。`index.js` 用纯函数
    `buildLatencyModel` 一次算清序列/丢包/缺口/时间范围再当参数传下去——此前 `latencyRange`
    与 `latencyLossSeries` 是渲染函数的副产品，而渲染有两处早返回，返回时统计读到的是**上一轮**
    的口径，画面与数字对不上且不报错。
  - `core/dom.js` 新增 `bindPopover`（点外部 / Esc 关闭），通知铃铛与「更多」共用；
    `.notify-popover` 的外壳样式提取为 `.popover`。

- **历史趋势改为「指标条 + 图」单卡**：原来两组按钮堆在卡片右上角占两行，图下方还有一排
  图例塞着六个数字。现在范围与视图合并成标题行右侧一条分段控件（`近6个月 | 近30天 |
  计费周期‖总计 | 详细`），图例里的数字上移进卡内指标条（区间总量 / 上行·下行 / 日均 /
  峰值 / 今日徽标），底部只在月度视图保留颜色图例。
  - **顺带修好三处**：① crosshair 插件的主题取值是首次建图时的闭包，走 `update` 分支不更新，
    切主题后准星颜色不跟随，改成绘制时现取；② 峰值标签一直没画出来——annotation 插件 v3 起
    `point` 注解不再支持内嵌 `label`（`line` 的仍有效），写在里面不报错也不渲染，改成独立的
    `label` 注解；③ 月度指标用的 `total_gb` 是后端 `toFixed` 过的**字符串**，参与运算会得到
    `NaN`，算数一律改走 `total`（字节）。
  - Y 轴按设计稿收到 46px，但只作下限（`Math.max(46, 自适应宽度)`）：硬钉死会把
    `100 GB` 这类满格刻度裁掉左半边。刻度限 5 格，且只有顶端那格带单位。
  - 空数据不再直接 return：接口返回 `null` 时归一成空数组，画一张空的折线图并跳过均值/峰值
    注解。此前分段控件已切到「近30天」，图上却还留着月度柱状图、指标条还写着「月均 / 峰值 03月」。
  - 新增共享原语 `.seg-control` / `.seg-item` / `.seg-sep`（③-a 预留、此处首次落地）与
    `.metric-bar.is-inline` 行内变体；新增 `.metric-bar-value.is-lead`。
  - 全局补 `[hidden] { display: none !important }`：UA 的 `[hidden]` 属于 user-agent 源，
    作者侧任何 `display: inline-flex` 都能压过它，JS 设了 `hidden` 元素照样显示且不报错
    （今日徽标就踩了这个）。原先分散在 `.uptime-pill[hidden]` / `.notify-popover[hidden]`
    的两条补丁一并收掉。

- **流量统计重做为两卡布局**：今日/昨日/本月三张卡合成左「本月总流量 + 计费配额」、
  右「按协议明细」两张。原布局把同一份数据按时间切成三块，看不出「这个月还剩多少额度」
  和「哪个协议在吃流量」——而这才是这个模块要回答的两个问题。
  - 左卡：本月总流量主数值 + 配额条（预警阈值刻度、越过最低阈值转红）+ 剩余额度 /
    日均消耗 / 周期末预估三栏指标（复用 `.metric-bar`）。预估超阈值转橙、超额度转红。
  - **主数值与配额用量不同源**，`tx_only` 等单向计费下必然不相等：前者是 `this_month`
    的双向总和，后者是按 `billing_mode` 折算的 `used_bytes`。所以配额那行标的是
    「计费用量」，剩余/日均/预估三栏也一律走计费口径，不与主数值混算。
  - 右卡：协议 × 今日/昨日/本月累计的四列表格，取代原先的端口列表；本月累计带一条占比条。
    占比条用配额条同款紫→青渐变而非按协议分色——紫/青已经是全站的上下行语义色。
    表格的「总计」行是整机口径（`/api/stats`），不是端口行之和：端口只统计 iptables
    记账的那几个，两者本来就对不上。
  - 计费周期（起始日、剩余天数、已过天数）改在前端按 `reset_day` 推算，与后端
    `GetBillingCycleDates` 同口径（钳到 1-28），**零后端改动**。
  - 未配置额度（`MONTHLY_LIMIT_GB=0`）时隐藏整块配额条，剩余额度显示 `--`，
    日均/预估仍成立。
  - 明细表 4 列全是 `nowrap`，列宽不够时是整行溢出卡片而不是换行：≤560px 缩一档、
    ≤440px 再缩一档并给表格兜一个横向滚动（数值长度取决于实际流量，靠断点算不死）。
    另外 `.cards` 的 `auto-fit` 下限必须写 `min(470px, 100%)`，只写 `470px` 时
    375px 视口整页会横向溢出 100 多像素。

- **实时网速改为镜像面积图**：中轴为 0，上行向上、下行向下，取代原先两条线叠在一起。
  卡片自上而下为实时读数行（34px 上下行读数 + 合计/峰值）、190px 图表、5 栏指标条
  （60s 上/下行均值、上/下行峰值、窗口累计）；标题行改为左标题右控件的一行式布局。
  - 负值只写进喂给 Chart.js 的派生数组，均值/峰值/累计/量程一律读真值序列——把负数写进
    真值数组会让量程比较漏掉全部下行点，窗口累计变成「上行减下行」。
  - 0 轴实线画在数据之上、±半程虚线画在数据之下：Chart.js 的 `grid.borderDash` 是整条轴
    一套样式，做不出两种虚实，改由自绘插件处理，颜色在绘制时现取，切主题自动跟随。
  - 新增共享原语 `.section-head` / `.head-pill` / `.metric-bar`，供后续模块复用；
    新增 `--speed-grid-axis`、`--divider-faint` 两个 token；区块间距 60px → 48px。

- **全站统一上下行语义色**：上行 / 上传 / TX 一律用紫（深色 `#b66cff`、浅色 `#8b5cf6`），
  下行 / 下载 / RX 一律用青（深色 `#4dd4ff`、浅色 `#0aa2c0`）。此前流量统计用绿/蓝、
  历史趋势用 `#4F7DF7`/`#39D0C3`、实时网速用紫/青，三套配色各说各话，同一个「上行」
  在三个模块里是三种颜色。`#30d158` 之后只表示「健康/正常」，不再兼任上行色。
  - 新增 `--axis-faint` token（轴标签与超小说明文字，比 `--muted` 再弱一档）。
  - 各模块不再散写十六进制字面量，统一走 `core/theme.js` 的 `palette()`；
    Chart.js 与 ECharts 的 tooltip 配色也收敛到 `tooltipColors()` 一处。
  - 延迟图的多目标色板、丢包色、缩放手柄、极值/均值标签底色改为从 token 派生，
    并移到渲染时求值，切换主题会立即跟随。

- **前端拆分为原生 ES Modules**：`web/app.js`（2571 行的单文件全局脚本）拆成 `web/js/`
  下的 13 个模块 —— `core/`（格式化、DOM、HTTP、SSE 总线、主题、图表库出口）与
  `modules/`（系统状态、实时网速、流量统计、历史趋势、延迟监控、通知）各司其职。
  不引入任何构建工具或框架，浏览器原生加载；类型检查由根目录 `jsconfig.json` 的
  `checkJs` + JSDoc 提供，仅作用于编辑器，不进 CI。本次 UI 零变化。
  - SSE 改为事件总线：系统状态订阅具名 `system` 事件、实时网速订阅默认消息，
    两者不再互相依赖，且每个订阅者独立捕获异常，一侧渲染出错不会连带停掉另一侧。
  - 断线重连由固定 5 秒改为 1→2→4→8→10 秒指数退避，首次重连更快。
  - `/api/traffic/ports`、`/api/traffic/monthly`、`/api/traffic/daily`、`/api/config`
    补齐 401 跳转登录页与响应校验，此前只有 stats/system/latency 三个端点有。

### 修复

- **切换主题后历史趋势图不跟着变色**：`applyTheme` 里硬编码的重绘列表漏了趋势图，
  网格线、tooltip 与 crosshair 会停在旧主题，要等下一次数据刷新（近 30 天/计费周期
  1 分钟、近 6 个月 1 小时）才纠正。改为各图表模块自行注册重绘回调，不会再漏。

- **375px 视口整页横向滚动**：历史趋势那条五格分段控件按默认内边距要 336px，比可用宽
  多 1px。≤440px 时收窄格内留白。

## [0.22.1] - 2026-08-01

### 修复

- **实时网速图鼠标悬停不动会拖出一串圆点**：Chart.js 把 hover 态记在 element 对象自身，
  而每秒 `shift()` 会让整排 element 左移一格；鼠标不动时它只按索引判断"激活的还是同一格"，
  于是既不清旧的、又给挪到这一格的新 element 补上高亮，一秒攒一个往左漂的空心圆点。现在
  推点前先摘掉上一秒那个 element 的 hover 样式，高亮和 tooltip 始终只落在光标压着的那一格，
  内容也跟着每秒刷新。

## [0.22.0] - 2026-07-31

### 变更

- **通知入口并入主机名胶囊**：配置了 Telegram 后不再单独占一枚「通知」胶囊，改为在
  主机名左侧长出一枚绿色小铃铛，点主机名即可开合原来的通知面板。顶栏在窄屏上少一个
  换行元素；原来胶囊上的「每日 HH:00」本就与面板里的每日报告状态重复，现在只保留面板
  一处（鼠标悬停胶囊也会显示）。未配置 Telegram 时胶囊不可点，与改动前的纯展示态一致。

## [0.21.0] - 2026-07-31

### 变更

- **系统状态区重构为 3×2 卡片网格**。磁盘、系统负载从上一版的「一行裸文字摘要」
  升回一级卡片：它们与卡片同为一级指标，却被降格成 13px 纯文本、又不对齐栅格，
  左顶右空破坏了区块节奏。运行时长不是指标而是上下文，移到页眉做成绿点胶囊，
  与服务器名/主题开关同族。
- 统一卡内结构：**标签 + 状态角标 / 主数值 / 进度条 / 副信息**。占用率类指标
  （CPU、内存、磁盘、负载）共用一套阈值——<70% 健康、70–90% 注意、>90% 告警，
  角标与进度条同色，不必盯着数字自己心算；负载先按核数换算成百分比再套阈值。
- 磁盘、内存副信息统一给出「N% 已用 · 可用 X」，磁盘的可用量仍取 `disk_avail`。
- 活跃连接新增按端口拆分的分段条，宽度即各端口占比；无连接的端口留最小占位块，
  表达「端口在但没人连」而非「没有这个端口」。
- CPU 的「宿主机抢占」提示并入副信息行（原为独立槽位），仍按近 1 分钟均值判定；
  只有抢占那一段染警告色，同行的核数是中性事实，不跟着变橙。
- **页眉改为左对齐单行**：标题与时间收到左侧，运行时长/服务器名/通知/主题控件在同
  一行右侧（此前是居中 48px 大标题 + 右上角浮层的两段式）。首屏因此多出约 130px
  可视高度，进页面就能看到实时网速。标题降到 40px，仍明显大于 32px 的区块标题。

## [0.20.0] - 2026-07-31

### 新增

- **系统状态卡片重排**，替换为与代理服务更相关的指标：
  - 新增 **活跃连接数**（解析 `/proc/net/tcp{,6}` 统计各代理端口的 ESTABLISHED
    连接），直接反映「此刻有多少人在用」，连接异常暴涨也一眼可见。
  - 新增 **TCP 重传率**（`/proc/net/snmp` 的 `RetransSegs`/`OutSegs` 差值），
    比 CPU 更能解释「代理为什么卡」，与延迟监控互补：延迟正常但重传高即线路丢包。
  - 磁盘、系统负载、运行时长降级为一行摘要——变化以天计，不必占用需要频繁扫视的
    卡片位；负载同时标注 CPU 核数，数值终于有了参照系。
  - CPU 卡片在 steal 近 1 分钟均值超过 1% 时显示「宿主机抢占」角标；判定用均值
    而非瞬时值，否则单轮抖动会让角标反复闪烁。
- 实时网速图表支持悬停查看具体数值：tooltip 显示该秒的时间戳与上传/下载速度，
  样式与主题联动。此前 tooltip 被禁用，鼠标悬停只出现两个圆点而看不到数值。

### 修复

- **CPU 使用率计算修正**（此前在三处偏离标准口径）：
  - `iowait` 被算作「使用中」，磁盘 IO 繁忙时使用率虚高；现按 htop/gopsutil 的口径
    与 `idle` 一同视为空闲。
  - `guest`/`guest_nice` 被重复累加——内核已把它们分别计入 `user`/`nice`，
    重复相加会让分母偏大、使用率偏低；现只累加到 `steal` 为止。
  - `/proc/stat` 字段解析失败时静默当作 0，会算出错误的使用率；现整轮采样丢弃。
- 磁盘剩余容量新增 `disk_avail` 字段，取 `Bavail`（普通用户可写容量）而非
  `total - used`，后者包含 ext4 默认预留给 root 的 5%，会高估可用空间。
- 实时网速图表在鼠标悬停期间每秒刷新会把 hover 样式留在旧数据点上，移开鼠标后
  残留一串圆点；数据更新改用 `chart.update("none")`。
- 实时网速图表启动时用 `null` 而非 `0` 填充未采集的 60 秒窗口，避免把"还没有数据"
  画成一条 0 速度的实线；平均速率相应只对已采到的点求平均，不再被占位值拉低。

### 优化

- 新增 `steal_percent` 指标：CPU 被宿主机抢占的占比，持续偏高即说明 VPS 所在物理机
  超售——此前这部分时间混在 CPU 使用率里，会被误判成自身负载高。
- `/api/system` 新增 `cpu_cores`（负载数值的参照系）与 `uptime_sec` 字段。
- 系统状态改由 SSE 的具名事件 `system` 推送（复用实时网速那条长连接），前端不再
  单独轮询 `/api/system`。推送按快照时间戳变化触发而非固定 5 秒 ticker：独立 ticker
  与采集周期不同相位，页面数据会平白多陈旧最多 5 秒；现在采集完 1 秒内即送达。
- 采集器首次采样无法算出 CPU 使用率时，`cpu_percent` 返回 `null` 而非伪造的 `0`，
  前端显示占位符。
- 系统资源指标（CPU/内存/磁盘/负载）改为只存内存快照，不再写入 SQLite。此前每 5 秒
  INSERT 一行、每 5 分钟 DELETE 一批，而查询只取最新一行——历史数据从未被使用，
  纯属空耗 VPS 的磁盘 IO。`system_metrics` 表与其索引一并移除（迁移时自动 DROP）。
- 服务启动时立即采集一次系统资源，不必再等首个 5 秒周期，`/api/system` 不会短暂 404。
- 连接数扫描降频到 15 秒一次（其余轮次复用上轮结果）：`/proc/net/tcp` 是 seq_file
  全表遍历且内核持锁，连接数上万时每 5 秒扫一次等于让监控自己加重被监控机器的负载，
  而「此刻有多少人在用」本就不需要 5 秒粒度。
- `/proc/net/tcp{,6}` 读到一半出错时不再静默少算，改为整轮作废：`conns_total`
  输出 `null`、前端显示 `--`，避免把偏低的连接数当成真值展示。

## [0.19.0] - 2026-07-31

### 新增

- PWA 支持：新增 `manifest.json`、Service Worker 与 192/512 应用图标，面板可作为
  独立窗口的桌面/移动 Web App 安装。Service Worker 采用网络优先策略，API 与 SSE
  不经拦截以保证监控数据实时性，断网时回退到缓存的静态外壳。
- `theme-color` 随浅色/深色主题切换同步更新，独立窗口标题栏不再固定为深色。
- 页面内边距加入 `env(safe-area-inset-*)`，iOS 独立窗口下内容不再被状态栏遮挡。

### 优化

- 内嵌静态资源改为带 `ETag` 并交由 `http.ServeContent` 处理条件请求。此前 HTML/CSS/JS
  只有 `Cache-Control: no-cache` 而无校验器，浏览器无法重验证，每次打开页面都要重传
  `vendor/` 下约 1.3 MB 的图表库；现在命中时返回 304。
- 图标与 SVG 增加 `Cache-Control: max-age=86400`。
- 三张 PNG 图标去掉全不透明的冗余 alpha 通道并重新压缩（无损），合计 184 KB → 79 KB。

## [0.18.2] - 2026-07-26

### 修复

- `RESET_DAY` 设为 29-31 时计费周期起点错乱：短月不存在该日期，`time.Date` 会把
  「2 月 31 日」规范化成 3 月 3 日，导致周期起点跳到未来、用量统计归零。现于
  配置加载与计算时统一钳制到 1-28 并打印告警。
- `GetBillingCycleDates` 未把传入时间换算到配置时区，跨时区调用会取错日历日。
- 总流量偏移量 `totalTxOffset`/`totalRxOffset` 在实时网速协程（每秒读）与流量采集
  协程（每分钟写）之间无同步，构成数据竞争；改用 `atomic.Uint64`（Darwin mock 的
  假计数器同源问题一并修复）。
- 每日 Telegram 报告在系统时钟回拨时可能于同一整点重复推送：下一次推送时刻改为以
  「本次目标时刻」为基准推算。

### 变更

- 系统指标清理由每 5 秒一次 `DELETE` 改为每 5 分钟一次，避免高频删除反复重建
  `idx_system_metrics_ts` 造成的 WAL 写放大；删除失败不再静默忽略。

## [0.18.1] - 2026-07-26

### 修复

- Telegram 每日报告的延迟小节丢包率恒为 `丢0%`：整数格式化把全天丢包率四舍五入
  抹平（默认每分钟 5 包、单目标约 7200 包/天，要丢满 36 个包才够 0.5%），改为保留
  两位小数并对非零极小值显示 `<0.01%`。与 Web 页面口径一致（同为 `SUM(lost)/SUM(sent)`）。
- 同一小节中目标整天 100% 丢包时，因 `AVG(rtt_ms)` 为 NULL 被误显示为「无数据」，
  最严重的故障反而看不到丢包率；现输出「全程无响应 丢100.00%」。

## [0.18.0] - 2026-07-14

### 变更

- Telegram 通知（流量预警/每日报告/延迟小节）排版从 `<pre>` 等宽块改为
  `<blockquote>` 引用块分组：消除 macOS 桌面客户端上的代码块底色、复制按钮
  与 CJK 等宽对齐错位问题，各端渲染观感一致；进度条字符 `█░` 换为 `▰▱`，
  延迟小节改为可折叠引用块并将采样时段并入小节标题。

## [0.17.3] - 2026-07-11

### 修复

- 默认时区从 `Asia/Shanghai` 改为 `Asia/Singapore`（`HELIOX_MON_TZ` 默认值），
  影响计费周期重置、每日报告整点对齐等；时区加载失败时的兜底固定偏移标签由
  易混淆的 `"CST"` 改为中性的 `"+08"`（偏移量本身不变，新加坡与上海同为 UTC+8
  无夏令时）。
- 前端延迟监控日期选择器 `formatDateValue` 改用浏览器本地日历日而非
  `toISOString()` 的 UTC 日历日：此前在 UTC+8 时区凌晨 0-8 点访问页面时，
  「今天」会被算成前一天，导致翻页/日期上限校验出错。

## [0.17.2] - 2026-07-09

### 修复

- 延迟趋势图放宽「无数据」判定阈值（从 1.5×粒度改为「一个粒度桶 + 90s 余量」）：
  采集节奏未对齐整分钟、偶尔某分钟落 0 个样本形成的单桶假空洞（ping 并未真丢），
  不再被误标为「无数据」；细粒度下需连续缺 ≥2 分钟才标灰，粗粒度下真实的多分钟
  中断仍照常显示。

## [0.17.1] - 2026-07-09

### 修复

- 延迟趋势图的「无数据」灰色区域左边缘往内缩了一格，未完整覆盖缺口起点；改为
  贴齐缺口前最后一个真实数据点，灰色区域两端与断线严丝合缝。

## [0.17.0] - 2026-07-09

### 新增

- 客户端延迟上报：新增 `POST /api/latency/report`（独立 `HELIOX_MON_REPORT_TOKEN`
  Bearer 认证，与登录密码隔离），客户端可上报到 VPS 的端到端 RTT。数据以
  `client:<name>` 命名空间写入现有 `latency_records`，完全复用降采样、保留策略与前端
  图表，效果等价于在 `PING_TARGETS` 无缝多一项。附参考客户端 `scripts/latency-client.sh`。
- 高精度测延端点 `GET /api/echo`：免认证、不写库、不打日志的 204 回显，供客户端在
  keep-alive 连接上测量端到端 TCP 净 RTT，排除服务端业务耗时干扰。

## [0.16.1] - 2026-07-09

### 修复

- CI/发布工作流的 setup-go 增加 `check-latest: true`，强制使用最新 Go 补丁版；
  用 go1.25.12 重建二进制，修复标准库 crypto/tls 漏洞 GO-2026-5856
  （0.16.0 由带漏洞的 go1.25.11 构建）。

## [0.16.0] - 2026-07-09

### 新增

- 延迟趋势图标注无数据缺口（Smokeping 风格）：监控停采（如服务器重启）时段折线
  断开并覆盖灰色「无数据」区域，不再用直线跨越缺口造成误导；tooltip 区分
  「无数据」（停采）与「丢包」（探测超时）。

### 修复

- 丢包「异常时长」统计不再把无数据缺口时段误计入（缺口前恰好是高丢包点时，
  原逻辑会把整段缺口算作异常持续时间）。

## [0.15.5] - 2026-07-04

### 变更

- 网络延迟小节改用「首行放实际采样时段」彻底解决 Telegram 复制按钮遮挡：首行
  `采样 HH:MM–HH:MM` 去替真实延迟数据「挡枪」，下方数据行不再被右上角复制按钮遮住，
  同时顺带展示昨日探测的真实时间覆盖。移除 0.15.4 的点号锚点。

## [0.15.4] - 2026-07-04

### 变更

- 网络延迟小节的行尾锚点由「空格 + 连字符」改为「空格 + 点号」，观感更和谐。

## [0.15.3] - 2026-07-04

### 变更

- 每日报告网络延迟小节恢复用 `<pre>` 等宽块（0.15.2 改的普通富文本行观感不佳）。
  改用「行尾锚点」解决复制按钮遮挡：给首行末尾追加一个连字符（非空格、不会被
  Telegram trim），正好落在按钮下被遮住，把真实数据顶出遮挡区。

## [0.15.2] - 2026-07-04

### 修复

- 每日报告网络延迟小节行尾（如「丢0%」）仍被 Telegram 代码块复制按钮遮挡：0.15.1
  的「行尾补空格」无效（客户端会 trim 行尾空格，且复制按钮定位在块右上角、遮挡的
  是首行）。改为该小节不再用 `<pre>` 等宽块，用加粗标签的普通富文本行（无复制按钮），
  数值以 `·` 分隔。

## [0.15.1] - 2026-07-04

### 修复

- 每日报告网络延迟小节的行尾（如「丢0%」）被 Telegram `<pre>` 代码块右上角复制
  按钮遮挡：行尾补留白撑宽代码块，让文字与按钮错开。

### 变更

- 网络延迟小节去掉丢包 >0 的 ⚠️ 图标（视觉更协调），仅保留「丢N%」文字。

## [0.15.0] - 2026-07-04

### 新增

- 通知弹层新增「发送日报预览」按钮（`POST /api/notify/daily-report`）：手动触发一次
  每日报告，便于在页面查看真实格式与内容。不影响自动调度（日报调度是纯内存定时器，
  本次发送不写「已发送」标记）；未配置 Telegram 时如实回显「未配置」。

## [0.14.0] - 2026-07-04

### 新增

- 每日流量报告新增**网络延迟**小节：按 `PING_TARGETS` 逐目标汇报昨日平均 RTT、
  最低 RTT 与丢包率（口径与「昨日用量」一致，昨日整天）；丢包 >0 标注 ⚠️，
  全丢包/无数据目标显示「无数据」。复用已有探测数据，无需新增配置。

### 变更

- Telegram 通知改用 HTML 富文本（`parse_mode=HTML`）：标题加粗，数据行放等宽
  `<pre>` 块对齐（修复此前纯文本在手机非等宽字体下对不齐的问题），收敛 emoji 为
  一套克制的语义符号并移除装饰性分隔线与标题图标。服务器名、探测目标名等用户可控
  字符串统一做 HTML 转义。日报 / 流量预警 / 测试三类消息一并改版。

## [0.13.0] - 2026-07-02

### 变更

- 每日流量报告全面改版，提升信息量与可读性：
  - 新增**环比**——昨日合计相对前日的涨跌百分比。
  - 新增**周期进度条**、**日均用量**与**月底预测**（照当前速度推算周期末总量，
    可能超限时标注 ⚠️）。
  - 统一单位为 GiB（原限额标签写 GB 但按 1024³ 计算，实为 GiB），日期附中文星期。
- 每日报告调度增加日志：启动/每轮打印「下次推送时间」（含时区），发送成功也记一条；
  开启开关但未配 Telegram 时启动即告警，便于排查「配了却不发送」。

## [0.12.1] - 2026-07-01

### 变更

- 通知 UI 改版：移除底部「通知」区块，改为顶栏服务器名旁的胶囊（仅在配置了 Telegram
  时显示，每日报告开启时显示推送时刻）。点击胶囊弹出面板查看配置状态并发送测试消息，
  点击外部 / Esc 关闭。

## [0.12.0] - 2026-07-01

### 新增

- 每日流量报告：可选定时通过 Telegram 推送昨日用量 + 本计费周期累计/剩余/重置日。
  由 `DAILY_REPORT_ENABLED` 开关、`DAILY_REPORT_HOUR` 指定推送时刻（按时区整点，默认 9 点），
  定时器对齐到下一次触发，进程重启不重复推送。
- 页面「通知」区块：显示 Telegram 是否已配置、每日报告开关/时刻，并提供「发送测试消息」
  按钮即时验证配置（成功/失败原因回显）。新增 `POST /api/notify/test`，`GET /api/config`
  补充 `daily_report` 字段。

## [0.11.0] - 2026-07-01

### 新增

- 工程化质量门禁：新增 GitHub Actions CI（lint / vet / test / 多平台构建 / govulncheck）。
- `golangci-lint` 配置（errcheck、staticcheck、govet、bodyclose、errorlint 等）。
- 核心逻辑单元测试：计费周期计算、配置与端口解析、虚拟网卡过滤、SQLite 迁移与流量查询。
- 新增 `LICENSE`（MIT）与本 `CHANGELOG`。
- `http.Server` 读取超时（`ReadHeaderTimeout` / `ReadTimeout` / `IdleTimeout`），
  防御 Slowloris 慢速连接；SSE 实时推送不设 `WriteTimeout` 以免长连接被掐断。

### 修复

- 显式处理此前被忽略的错误返回值（`rows.Scan` / `json.Encode` / `w.Write` /
  `db.Exec` / `server.Shutdown` 等），消除静默吞错。
- `make fmt` 在未安装 `goimports` 时报错：改用 `go run` 临时拉取；补全 Makefile `.PHONY` 目标。

### 变更

- 前端图表库（Chart.js / annotation 插件 / ECharts）由 CDN 改为本地 `go:embed` 内嵌，
  消除墙内 CDN 加载失败导致图表空白的隐患。

## [0.10.43] - 2026-07-01

### 变更

- 前端细节优化：主题首屏跟随系统偏好、登录页支持浅色主题、innerHTML 注入转义、
  `formatSpeed` 去重、移除调试日志。

## [0.10.42] - 2026-07-01

### 变更

- 内嵌图表库去除 CDN 依赖并清理死代码（未使用的 CSS、登录页失效的 shake 动画）。

## [0.10.41] - 2026-07-01

### 修复

- 修正 SQLite DSN 参数格式，解决 `database is locked`。

## [0.10.40] - 2026-07-01

### 新增

- 提升延迟与丢包统计准确性，新增最小 RTT 与抖动（mdev）指标。

[Unreleased]: https://github.com/hh-io/heliox-mon/compare/v0.11.0...HEAD
[0.11.0]: https://github.com/hh-io/heliox-mon/compare/v0.10.43...v0.11.0
[0.10.43]: https://github.com/hh-io/heliox-mon/compare/v0.10.42...v0.10.43
[0.10.42]: https://github.com/hh-io/heliox-mon/compare/v0.10.41...v0.10.42
[0.10.41]: https://github.com/hh-io/heliox-mon/compare/v0.10.40...v0.10.41
[0.10.40]: https://github.com/hh-io/heliox-mon/compare/v0.10.39...v0.10.40
